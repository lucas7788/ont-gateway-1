package forward

import (
	"testing"
)

func TestPostAkSkRequestByName(t *testing.T) {
	// host := "192.168.1.219:8080"
	// uri := "/"
	// data := 2

	// code, _, _, err := PostAkSkRequestByName("ont-gw", host, uri, data)
	// assert.Assert(t, err == nil && code == 200, fmt.Sprintf("code %v err:%v", code, err))
}
