package middleware

import "github.com/gin-gonic/gin"

// AddonForward for forward addon request
func AddonForward(c *gin.Context) {
	// addonID := c.GetHeader("addonID")
	// tenantID := c.GetHeader("tenantID")

}
